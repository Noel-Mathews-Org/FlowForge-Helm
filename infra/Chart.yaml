apiVersion: v2
name: infra
description: Infrastructure deployment chart for FlowForge (Prometheus, Grafana, Ingresses)
type: application
version: 1.0.0
appVersion: "1.0.0"
dependencies:
  - name: kube-prometheus-stack
    version: "66.3.0"
    repository: "https://prometheus-community.github.io/helm-charts"

