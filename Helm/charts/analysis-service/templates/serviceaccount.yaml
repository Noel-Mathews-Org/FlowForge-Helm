apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ .Release.Name }}-{{ .Chart.Name }}
  namespace: {{ .Release.Namespace }}
  annotations:
    {{- if .Values.aiManagedIdentityClientId }}
    azure.workload.identity/client-id: {{ .Values.aiManagedIdentityClientId | quote }}
    {{- end }}
automountServiceAccountToken: false
