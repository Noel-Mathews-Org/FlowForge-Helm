apiVersion: v1
kind: Namespace
metadata:
  name: {{ .Values.global.namespace | default .Release.Namespace }}
  labels:
    name: {{ .Values.global.namespace | default .Release.Namespace }}
    app.kubernetes.io/managed-by: Helm
