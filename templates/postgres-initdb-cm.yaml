apiVersion: v1
kind: ConfigMap
metadata:
  name: postgres-initdb
  namespace: {{ .Release.Namespace }}
data:
  initdb.sql: |
    CREATE DATABASE auth_db;
    CREATE DATABASE project_db;
    CREATE DATABASE task_db;
    CREATE DATABASE analytics_db;
    
    \c auth_db
    
    DO $$ 
    BEGIN
        IF NOT EXISTS (SELECT 1 FROM pg_type WHERE typname = 'user_role') THEN
            CREATE TYPE user_role AS ENUM ('admin', 'manager', 'member');
        END IF;
    END $$;
    
    CREATE TABLE IF NOT EXISTS users (
        id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
        email VARCHAR(255) UNIQUE NOT NULL,
        hashed_password VARCHAR(255) NOT NULL,
        full_name VARCHAR(255) NOT NULL,
        role user_role NOT NULL,
        org VARCHAR(255) NOT NULL DEFAULT 'stratum',
        is_active BOOLEAN NOT NULL DEFAULT TRUE,
        created_at TIMESTAMP WITH TIME ZONE NOT NULL DEFAULT NOW()
    );
    
    INSERT INTO users (email, hashed_password, full_name, role, org) VALUES
    ('admin@stratum.com',   '$2b$12$tQAsvk..7eZ7z.6tU4oo2uCHc3p6wEEF0w/wF18KXXL/PBZJbeRn6', 'Admin User',   'admin',   'stratum'),
    ('manager@stratum.com', '$2b$12$tQAsvk..7eZ7z.6tU4oo2uCHc3p6wEEF0w/wF18KXXL/PBZJbeRn6', 'Manager User', 'manager', 'stratum'),
    ('user@stratum.com',    '$2b$12$tQAsvk..7eZ7z.6tU4oo2uCHc3p6wEEF0w/wF18KXXL/PBZJbeRn6', 'Regular User', 'member',  'stratum')
    ON CONFLICT (email) DO NOTHING;
