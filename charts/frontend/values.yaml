replicaCount: 2

image:
  repository: ghcr.io/noel-mathews-org/flowforge/frontend
  tag: latest
  pullPolicy: IfNotPresent

service:
  port: 3000

resources:
  requests:
    cpu: 100m
    memory: 96Mi

hpa:
  minReplicas: 2
  maxReplicas: 4
  targetCPUUtilizationPercentage: 70
