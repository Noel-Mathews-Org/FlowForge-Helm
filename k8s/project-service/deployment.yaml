apiVersion: apps/v1
kind: Deployment
metadata:
  name: flowforge-project-service
  namespace: default
spec:
  replicas: 2
  selector:
    matchLabels:
      app: flowforge-project-service
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxUnavailable: 50%
      maxSurge: 50%
  template:
    metadata:
      annotations:
        instrumentation.opentelemetry.io/inject-python: "true"
      labels:
        tier: backend
        app: flowforge-project-service
        azure.workload.identity/use: "true"
    spec:
      serviceAccountName: flowforge-project-service
      securityContext:
        runAsNonRoot: true
        runAsUser: 1001

      containers:
        - name: project-service
          image: "flowforgeacrm9mp04.azurecr.io/flowforge/project-service:07f448b"
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 8002

          envFrom:
            - configMapRef:
                name: flowforge-config

          env:
            - name: APP_PORT
              value: "8002"
            - name: GATEWAY_PORT
              value: "8002"
          resources:
            requests:
              cpu: 100m
              memory: 128Mi
          livenessProbe:
            httpGet:
              path: /health
              port: 8002
            initialDelaySeconds: 15
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /health
              port: 8002
            initialDelaySeconds: 5
            periodSeconds: 10

      nodeSelector:
        agentpool: node1
