apiVersion: apps/v1
kind: Deployment
metadata:
  name: flowforge-task-service
  namespace: default
spec:
  replicas: 2
  selector:
    matchLabels:
      app: flowforge-task-service
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxUnavailable: 50%
      maxSurge: 50%
  template:
    metadata:
      annotations:
        instrumentation.opentelemetry.io/inject-python: "true"
      labels:
        tier: backend
        app: flowforge-task-service
        azure.workload.identity/use: "true"
    spec:
      serviceAccountName: flowforge-task-service
      securityContext:
        runAsNonRoot: true
        runAsUser: 1001

      containers:
        - name: task-service
          image: "flowforgeacrm9mp04.azurecr.io/flowforge/task-service:07f448b"
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 8003

          envFrom:
            - configMapRef:
                name: flowforge-config

          env:
            - name: APP_PORT
              value: "8003"
            - name: GATEWAY_PORT
              value: "8003"
          resources:
            requests:
              cpu: 100m
              memory: 128Mi
          livenessProbe:
            httpGet:
              path: /health
              port: 8003
            initialDelaySeconds: 15
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /health
              port: 8003
            initialDelaySeconds: 5
            periodSeconds: 10

      nodeSelector:
        agentpool: node1
