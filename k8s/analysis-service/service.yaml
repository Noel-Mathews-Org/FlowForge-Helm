apiVersion: v1
kind: Service
metadata:
  name: analysis-service
  namespace: default
spec:
  selector:
    app: flowforge-analysis-service
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8004
